{
  "name": "uniform",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild index.js --format=esm --bundle --minify --outfile=out.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "0.24.2"
  },
  "dependencies": {
    "left-pad": "^1.3.0"
  }
}
